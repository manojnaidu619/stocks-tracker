<div class="container" align="center">
  <h1>New stocks-tracker app</h1>
  <% if !user_signed_in? %>
   <%= link_to "SIGNUP", new_user_registration_path %>
  <%  end %>
  <% if user_signed_in? %>
   <%= link_to "LOGOUT", destroy_user_session_path, method: :delete %>
  <%  end %>

</div>
